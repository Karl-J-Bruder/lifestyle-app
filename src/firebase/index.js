// import instance of Firebase class into firebase/index.js for simpler importing elsewhere
// e.g. import "/firebase" vs. "/firebase/firebase"

import firebase from "./firebase";
import FirebaseContext from "./context";

export { FirebaseContext };
export default firebase;